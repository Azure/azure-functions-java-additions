jobs:
  - job: "Build"
    displayName: 'Build java core library'

    pool:
      name: 1es-pool-azfunc-public
      image: 1es-windows-2022 
      os: windows

    steps:
    - task: JavaToolInstaller@0 # Java 17 required for azure-functions-java-mcp module (MCP SDK uses sealed interfaces/records)
      inputs:
        versionSpec: '17'
        jdkArchitectureOption: 'x64'
        jdkSourceOption: 'PreInstalled'
      displayName: 'Setup Java 17'
    - pwsh: |
        Write-Host "Java_HOME: $JAVA_HOME"
        Get-Command mvn
      displayName: 'Check Maven is installed'
    - pwsh: '& .\build.ps1'
      displayName: 'Build project with java core library'